import React from 'react'

const AccordionItemDescription = ({children}) => {
  return (
    <p className="accordionItemDescription">
      {children}
    </p>
  )
}

export default AccordionItemDescription
